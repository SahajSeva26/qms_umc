// Re-exported for existing in-feature imports — the canonical component now
// lives in components/ui/ChipPicker.tsx so other features (e.g. projects) can
// use it without reaching into the CRM feature's internals.
export { default } from '@/components/ui/ChipPicker'
