// Shared field/label styling for the New Lead wizard, matching the
// prototype's injectCss() module CSS exactly (crm-sales-leads.js
// .qms-sl-modal label / .input / .select / .textarea rules — byte-identical
// to the Projects wizard's .qms-prj-modal rules of the same name).
export const labelClasses = 'block text-[11px] font-bold uppercase tracking-wide mb-1'
export const labelStyle = { color: 'var(--qms-text-soft)' }
export const fieldClasses = 'text-sm rounded-[14px] px-3.5 py-3 h-auto'
